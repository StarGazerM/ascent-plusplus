
use proc_macro2::TokenStream;
use crate::syntax::{SlogProgram, SlogProgramLine};


// fn expand_macro_sexpr(sexpr: &SlogSExprClause) -> Option<TokenStream> {
//     if sexpr.is_macro {

//     } else {
//         None
//     }
// }

// pub fn expand_macro(program: &SlogProgram) -> Option<TokenStream> {
//     // expand the macro in the program, if needed
//     for line in &program.lines {
//         match line {
//             SlogProgramLine::Fact(fact) => {
//                 if fact.is_macro {

//                 }
//             }
//         }
//     }

//     None
// }

