[package]
name = "bench_doop"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
ascent = { path = "../../ascent" }
bench_loader = { path = "../bench_loader" }

[[bin]]
name = "doop_batch"
path = "src/doop_batch.rs"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
